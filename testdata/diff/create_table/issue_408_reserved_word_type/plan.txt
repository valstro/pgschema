Plan: 2 to add.

Summary by type:
  types: 1 to add
  tables: 1 to add

Types:
  + user

Tables:
  + mytable

DDL to be executed:
--------------------------------------------------

CREATE TYPE "user" AS (id uuid);

CREATE TABLE IF NOT EXISTS mytable (
    id uuid DEFAULT gen_random_uuid(),
    entered_by "user"
);
